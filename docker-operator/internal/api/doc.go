// Package api serves the docker-operator's REST surface -- the agent
// collection and item endpoints, the terminal-output endpoint, and the
// shared JSON error envelope every handler returns on failure.
//
// Scaffold only (issue #61); the handlers and the error envelope land in a
// later task.
package api
