---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: servicesets.sandbox.psenna.dev
spec:
  group: sandbox.psenna.dev
  names:
    categories:
    - sandbox
    kind: ServiceSet
    listKind: ServiceSetList
    plural: servicesets
    shortNames:
    - sbset
    singular: serviceset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ServiceSetSpec declares the long-lived dependency services and dev-tool
              runtimes for one SandboxEnvironment. The ServiceSetReconciler reconciles each
              entry to native Pods/Services/PVCs in the ServiceSet's namespace.
            properties:
              environmentName:
                description: |-
                  environmentName is the SandboxEnvironment this set belongs to. Used to
                  derive the shared workspace PVC name (<environmentName>-workspace) that
                  runtime pods mount.
                minLength: 1
                type: string
              runtimes:
                description: |-
                  runtimes are long-lived dev-tool pods the agent execs into. They mount the
                  shared workspace PVC and have no Service.
                items:
                  description: RuntimeSpec describes one long-lived dev-tool pod the
                    agent execs into.
                  properties:
                    args:
                      items:
                        type: string
                      type: array
                    command:
                      items:
                        type: string
                      type: array
                    dependsOn:
                      items:
                        type: string
                      type: array
                    env:
                      additionalProperties:
                        type: string
                      type: object
                    healthcheck:
                      description: HealthcheckSpec maps to a k8s readinessProbe. Exactly
                        one of exec/http/tcp.
                      properties:
                        exec:
                          items:
                            type: string
                          type: array
                        http:
                          properties:
                            path:
                              type: string
                            port:
                              format: int32
                              type: integer
                          required:
                          - path
                          - port
                          type: object
                        interval:
                          description: interval defaults to 5s when empty.
                          type: string
                        tcp:
                          properties:
                            port:
                              format: int32
                              type: integer
                          required:
                          - port
                          type: object
                      type: object
                    image:
                      type: string
                    mountWorkspace:
                      default: true
                      description: |-
                        mountWorkspace mounts the shared workspace PVC at /workspace. Defaults to
                        true when omitted.
                      type: boolean
                    name:
                      maxLength: 63
                      minLength: 1
                      type: string
                    resources:
                      description: ResourceRequirements describes the compute resource
                        requirements.
                      properties:
                        claims:
                          description: |-
                            Claims lists the names of resources, defined in spec.resourceClaims,
                            that are used by this container.

                            This field depends on the
                            DynamicResourceAllocation feature gate.

                            This field is immutable. It can only be set for containers.
                          items:
                            description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                            properties:
                              name:
                                description: |-
                                  Name must match the name of one entry in pod.spec.resourceClaims of
                                  the Pod where this field is used. It makes that resource available
                                  inside a container.
                                type: string
                              request:
                                description: |-
                                  Request is the name chosen for a request in the referenced claim.
                                  If empty, everything from the claim is made available, otherwise
                                  only the result of this request.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Limits describes the maximum amount of compute resources allowed.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Requests describes the minimum amount of compute resources required.
                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                      type: object
                    runAsUser:
                      format: int64
                      type: integer
                  required:
                  - image
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              services:
                description: |-
                  services are long-lived dependency pods. Each gets a Service (cluster
                  DNS <name>.<ns>.svc) and, if storage is set, a retained data PVC.
                items:
                  description: ServiceSpec describes one long-lived dependency pod.
                  properties:
                    args:
                      items:
                        type: string
                      type: array
                    command:
                      items:
                        type: string
                      type: array
                    dependsOn:
                      description: dependsOn names other service/runtime entries that
                        must be Ready first.
                      items:
                        type: string
                      type: array
                    env:
                      additionalProperties:
                        type: string
                      type: object
                    envFromSecret:
                      description: envFromSecret references a Secret name whose keys
                        become env vars.
                      type: string
                    expose:
                      description: expose, if set, publishes the first port as a NodePort
                        on this host port.
                      format: int32
                      type: integer
                    healthcheck:
                      description: HealthcheckSpec maps to a k8s readinessProbe. Exactly
                        one of exec/http/tcp.
                      properties:
                        exec:
                          items:
                            type: string
                          type: array
                        http:
                          properties:
                            path:
                              type: string
                            port:
                              format: int32
                              type: integer
                          required:
                          - path
                          - port
                          type: object
                        interval:
                          description: interval defaults to 5s when empty.
                          type: string
                        tcp:
                          properties:
                            port:
                              format: int32
                              type: integer
                          required:
                          - port
                          type: object
                      type: object
                    image:
                      type: string
                    imagePullPolicy:
                      description: PullPolicy describes a policy for if/when to pull
                        a container image
                      type: string
                    name:
                      maxLength: 63
                      minLength: 1
                      type: string
                    ports:
                      items:
                        format: int32
                        type: integer
                      type: array
                    resources:
                      description: ResourceRequirements describes the compute resource
                        requirements.
                      properties:
                        claims:
                          description: |-
                            Claims lists the names of resources, defined in spec.resourceClaims,
                            that are used by this container.

                            This field depends on the
                            DynamicResourceAllocation feature gate.

                            This field is immutable. It can only be set for containers.
                          items:
                            description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                            properties:
                              name:
                                description: |-
                                  Name must match the name of one entry in pod.spec.resourceClaims of
                                  the Pod where this field is used. It makes that resource available
                                  inside a container.
                                type: string
                              request:
                                description: |-
                                  Request is the name chosen for a request in the referenced claim.
                                  If empty, everything from the claim is made available, otherwise
                                  only the result of this request.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Limits describes the maximum amount of compute resources allowed.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Requests describes the minimum amount of compute resources required.
                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                      type: object
                    runAsUser:
                      format: int64
                      type: integer
                    storage:
                      description: |-
                        ServiceStorageSpec creates a per-service RWO data PVC, retained by name across
                        Pod recreates.
                      properties:
                        mountPath:
                          description: mountPath is the in-container mount point for
                            the data PVC.
                          type: string
                        size:
                          description: size is a quantity string, e.g. "1Gi".
                          type: string
                      required:
                      - mountPath
                      - size
                      type: object
                  required:
                  - image
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - environmentName
            type: object
          status:
            description: ServiceSetStatus is the reconciled state of a ServiceSet.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              entries:
                items:
                  description: EntryStatus is the readiness of one service or runtime
                    entry.
                  properties:
                    kind:
                      description: kind is "service" or "runtime".
                      type: string
                    name:
                      type: string
                    ready:
                      type: boolean
                    reason:
                      type: string
                  required:
                  - kind
                  - name
                  - ready
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
