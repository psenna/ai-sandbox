# role.yaml (generated by `make manifests` from the +kubebuilder:rbac markers
# on internal/controller/sandboxenvironment_controller.go) and
# role_binding.yaml (hand-written -- controller-gen doesn't generate
# bindings) landed in #18, once the SandboxEnvironment controller existed to
# generate RBAC from.
resources:
  - service_account.yaml
  - leader_election_role.yaml
  - leader_election_role_binding.yaml
  - role.yaml
  - role_binding.yaml
